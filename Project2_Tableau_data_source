-- To visualize data in Tableau, I used below table containing the requests for the last 5 years.

SELECT
    created_date,
    closed_date,
    agency,
    agency_name,
    complaint_type,
    descriptor,
    street_name,
    address_type,
    city,
    status,
    resolution_description
FROM bigquery-public-data.new_york_311.311_service_requests
WHERE created_date > "2016-11-28"
